# 프론트엔드 개발 서버 CORS 허용 origin (쉼표로 여러 개 지정 가능)
FRONTEND_ORIGIN=http://localhost:3000

# (선택) 자연어 질의(/api/chat)를 SQL로 변환하는 데 사용할 OpenAI API 키.
# 설정하지 않으면 /api/chat은 단순 키워드 검색으로 자동 대체됩니다.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
